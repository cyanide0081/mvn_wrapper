wraps maven command line script setting JAVA_HOME and other relevant variables
based on your current project's pom.xml file(s).

## Building

Run this (currently only building for Windows using
[Clang](https://releases.llvm.org/download.html) is supported):
```
.\build.cmd
```
